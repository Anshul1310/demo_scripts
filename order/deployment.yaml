apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-deployment
  namespace: nittfest
  labels:
    app: order-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: order-service
  template:
    metadata:
      labels:
        app: order-service
    spec:
      containers:
        - name: order-service
          image: anshul8394/order-service:174
          ports:
            - containerPort: 8082
          envFrom:
            - configMapRef:
                name: nfconfigmap
